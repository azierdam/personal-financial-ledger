# Implementation Plan: C1.2 — Repository Contracts

## Objective
Establish storage-agnostic persistence interfaces for `Account` and `Category` entities and ensure consistency across all repository contracts.

## Key Changes
- `src/repository/AccountRepository.gs`: New interface definition.
- `src/repository/CategoryRepository.gs`: New interface definition.
- `src/repository/TransactionRepository.gs`: Ensure consistency with new interfaces.
- `test/unit/RepositoryContractTest.gs`: Unit tests for interface structures.

## Implementation Steps
1. Define `AccountRepository` and `CategoryRepository` interfaces supporting `save`, `findById`, `findAll`, `update`.
2. Review `TransactionRepository` for consistency with the new interfaces.
3. Implement unit tests that verify each interface supports the required CRUD methods.
4. Update `docs/03-Engineering/Repository_Pattern.md` (new doc) to define the interface contract standard.

## Verification
- Unit tests verify the existence and signature of CRUD methods for all three repositories.
- Code review ensures no storage-specific types or logic leaked into the repository contracts.
