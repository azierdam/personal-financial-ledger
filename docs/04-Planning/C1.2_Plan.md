# Implementation Plan: C1.2 — Repository Contracts

## Objective
Establish storage-agnostic persistence interfaces for `Account` and `Category` entities, enforce transaction immutability, and document the Repository Contract Standard.

## Key Changes
- `src/repository/AccountRepository.gs`: New interface (save, findById, findAll, update).
- `src/repository/CategoryRepository.gs`: New interface (save, findById, findAll, update).
- `src/repository/TransactionRepository.gs`: Refine interface (save, findById, findAll).
- `docs/03-Engineering/Repository_Contract_Standard.md`: New governance document.
- `test/unit/RepositoryContractTest.gs`: Unit tests to verify contract adherence.

## Implementation Steps
1. Define `Repository_Contract_Standard.md`.
2. Define `AccountRepository` and `CategoryRepository`.
3. Remove `update` from `TransactionRepository`.
4. Implement unit tests verifying contract methods exist (or don't) based on immutability.

## Verification
- Unit tests verify the existence and signature of CRUD methods for all three repositories.
- Code review ensures no storage-specific types leaked into repository contracts.
