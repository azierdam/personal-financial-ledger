# Implementation Plan: C2.2 — Transaction Persistence Flow

## Objective
Connect `TransactionService` to `TransactionRepository` adapters to enable transaction persistence.

## Key Changes
- `src/service/TransactionService.gs`: Refactor to use injected `TransactionRepository`.
- `test/integration/PersistenceFlowTests.gs`: New tests for end-to-end persistence flow (domain -> service -> repository).

## Implementation Steps
1. Refactor `TransactionService` to accept `TransactionRepository` in constructor.
2. Update service methods to delegate storage operations to the injected repository.
3. Implement `PersistenceFlowTests` to verify `TransactionService` interacts correctly with repository interfaces.
4. Update `docs/03-Engineering/Infrastructure_Architecture.md` to show flow.

## Verification
- Run persistence integration tests (using mocks/fakes for repository).
- Verify `TransactionService` logic remains storage-agnostic.
